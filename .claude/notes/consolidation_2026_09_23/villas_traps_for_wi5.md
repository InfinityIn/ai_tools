# Ловушки проекта villas — текст для переноса в репозиторий villas (WI-5)

Происхождение: раздел «Ловушки проекта villas (проверять актуальность)» из
`~/.claude/skills/develop-it/SKILL.md` (правка 2026-08-13, в репозиторий
ai_tools не возвращалась).

Почему вынесено сюда: репозиторий `ai_tools` **публичный**, а текст содержит
пути к кредам, имена хостовых ключей, порт дев-БД и имена живых процессов.
В `src/process/develop-it/SKILL.md` восстановлен обезличенный шаблон из main
(решение ревью WI-2, M1).

Куда девать: в репозиторий villas — в `docs/` (например, `docs/traps.md`),
со ссылкой из villas `CLAUDE.md`. Делается в WI-5 вместе с остальным
подключением villas. Перед переносом **проверить актуальность каждого
пункта** — часть могла устареть с августа 2026.

## Текст как есть

```markdown
## Ловушки проекта villas (проверять актуальность)

- Alembic: один head (CI-гейт), миграцию катить отдельным пушем до кода.
- Worktree не получает своего `villas_core` — editable install резолвится в
  основной checkout: пинить `PYTHONPATH`.
- Repo-root `.env` перебивает env тестов через `load_dotenv(override=True)` —
  пинить `DOTENV_PATH`; дев-БД проекта — 5434, не перепутать с прод.
- Хосты и креды: `.secrets/` (Trello), `~/.ssh/claude_deploy` (deploy@hosts),
  живые compose — в checkout self-hosted раннера, не /opt/villas.
- Скрейпер fb-loop не трогать (живая FB-сессия, single-session rule).
```

## Привязка к регламентам (для WI-5)

- пункт про Alembic → **R2.4** (модель данных и миграции), **R5.2**
  (миграции в проде);
- пункт про worktree и `PYTHONPATH` → **R3.5** (гигиена веток и рабочих
  деревьев агентов);
- пункт про `.env` / `DOTENV_PATH` / порт дев-БД → **R2.5** (конфигурация и
  флаги: один путь чтения);
- пункт про `.secrets/`, ssh-ключ и живые compose → **R0.5** (секреты,
  доступы, break-glass), **R5.1** (деплой: где живой compose);
- пункт про fb-loop → **R3.6** (права и действия агента).
