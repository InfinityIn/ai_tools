---
name: research-it
description: Сессия ресерча в режиме оркестрации — внешний мир, рынок, технологии, конкуренты. Запускать при старте ресерч-задачи: /research-it <вопрос или тема>.
---

# research-it — ресерч руками агентов

**База:** вызови скилл `orchestrate` и работай по его скелету; правила работы с
данными и формат финала — см. `orchestrate`. Ниже — профиль ресерча.

## Профильные правила

- **Многоканальный сбор**: параллельные агенты, каждый ищет своим каналом
  (веб-поиск, официальные доки, форумы/сообщества, соцсети/маркетплейсы) и слеп
  к находкам остальных — так каналы не заражают друг друга.
- **Каждый ключевой факт — ≥2 независимых источника**, у факта фиксируются
  ссылка и дата. Единственный источник — пометка «не подтверждено».
- Цифры (цены, лимиты, законы, API) не берутся из памяти модели — только из
  живых источников с датой.
- **Финальный гейт — адвокат дьявола**: отдельный агент атакует выводы
  (что устарело, что противоречит, какой канал не смотрели). Его находки —
  либо закрыть дополнительным сбором, либо честно внести в отчёт как риски.

## Артефакт

Отчёт в `.claude/notes/<тема>/`: выводы → факты со ссылками и датами →
непроверенное и риски.

## Регламенты

Каталог и индекс: `docs/regulations/README.md` в репозитории ai_tools
(https://github.com/InfinityIn/ai_tools). Ссылки — по коду.

- **R1.3** — discovery vs delivery: типы сессий, бюджет карточек на discovery
  (отчёт + ≤ 5 пакетов), WIP на обоих треках. Ресерч — это discovery-трек:
  он не порождает поток карточек.
- **R2.2** — ADR: решение, принятое по итогам ресерча, оформляется ДО
  реализации, а не задним числом.
